# Mapbox access token (public "pk." token is correct for a browser app).
# Get one at https://account.mapbox.com/access-tokens/
# Without this the app falls back to the keyless OSRM demo server (dev only).
VITE_MAPBOX_TOKEN=
